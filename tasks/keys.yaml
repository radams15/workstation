- name: Copy .ssh directory
  copy:
    src: "{{ mountpoint }}/keys/.ssh"
    dest: "{{ home_dir }}"
    
- name: Change private key permissions
  file:
    path: "{{ item }}"
    state: file
    mode: "0600"
  with_items:
    - "{{ home_dir }}/.ssh/id_rsa"
    - "{{ home_dir }}/.ssh/warwick_rsa"
